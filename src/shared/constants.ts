export const DEFAULT_CONFIG: ExtensionConfig = {
    visibleMessageLimit: 3,
    loadMoreBatchSize: 3,
    enabled: true,
    showStatus: true,
    statusPosition: "top-right",
    fetchInterceptEnabled: true,
    theme: "dark",
    autoLoad: false,
    weeklyRequestLimit: 3000,
    hideOldMessages: true,
};
export const DATA_ATTR = "data-acsb-managed";
export const CSS_PREFIX = "acsb";
export const MUTATION_DEBOUNCE_MS = 80;
